---

syslog:
  path: "/dev/log"
  perms: 0666
  filter:
    facility: "auth,authpriv"
  output:
    template: "[SYSLOG] %s"

pipes:
  -
    path: /docker.stdout
    type: stdout
    perms: 0666
  -
    path: /docker.stderr
    type: stderr
    perms: 0666
    output:
      template: "[STDERR] %s"
